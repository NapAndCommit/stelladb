---
export interface Props {
    title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width,initial-scale=1" />
        <link rel="preload" href="/font.woff2" as="font" type="font/woff2" crossorigin />
        <meta name="google-site-verification" content="QnPzHT-3k8oi0acqMvSozJjSGyAgimpbAop5WpEQ_Oc" />
        <title>{title}</title>
        <meta name="description" content="a lazy stella sora database" />
        <meta name="theme-color" content="#00a9ff" />
        <meta property="og:site_name" content="stelladb.pages.dev" />
        <link rel="canonical" href={Astro.url.href} />
        <meta property="og:image" content="https://stelladb.pages.dev/nazuna.png" />
        <link rel="sitemap" href="/sitemap-index.xml" />
    </head>
    <body>
        <nav>
            <a href="/" class="logo">stelladb</a>
            <div class="navlinks">
                <a href="/character">Character</a>
                <a href="/disc">Disc</a>
                <a href="/banner">Banner</a>
                <a href="/emblem">Emblem</a>
            </div>
        </nav>

        <main>
            <slot />
        </main>
    </body>
</html>

<style is:global>
    :root {
        --bg: #111;
        --bg1a: #1116;
        --bg2: #222;
        --bg3: #6df;
        --bg4: #ff6;
        --bg5: #f6d;
        --muted: #bbb;
        --text: #eee;
        --accent: #6f9;
        --nav-height: 64px;
    }

    html,
    body {
        margin: 0;
        min-height: 100%;
        scrollbar-gutter: stable;
    }

    body {
        background: var(--bg);
        color: var(--text);
        font-family: Inter, 'Segoe UI', Arial, system-ui, sans-serif;
    }

    a {
        color: var(--muted);
        text-decoration: none;
    }

    a:hover {
        color: var(--text);
    }

    button {
        font-family: inherit;
    }

    @font-face {
        src: url('/font.woff2') format('woff2');
        font-family: Inter;
        font-display: swap;
    }
</style>

<style>
    nav {
        display: flex;
        position: sticky;
        top: 0;
        align-items: center;
        z-index: 727;
        backdrop-filter: blur(5px);
        margin: 0 auto;
        background: var(--bg1a);
        padding: 0 1rem;
        max-width: 1000px;
        height: var(--nav-height);
    }

    .logo {
        margin-right: 1rem;
        padding: 0.5rem 0.5rem;
        color: var(--accent);
        font-weight: 700;
        font-size: 1.5rem;
        text-decoration: none;
    }

    .navlinks {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-left: auto;
    }

    .navlinks a {
        padding: 0.5rem 0.5rem;
    }

    main {
        margin: auto;
        padding: 0 1rem;
        max-width: 1000px;
    }
</style>
