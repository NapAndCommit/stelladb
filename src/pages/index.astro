---
import Layout from '../layouts/Layout.astro';
---

<Layout title="stelladb">
    <div class="center">
        <h1>the worst stella sora database</h1>
        <p>100% text, 0% image</p>
    </div>

    <footer>
        <p>read if cute and funny</p>
    </footer>
</Layout>

<style>
    .center {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: calc(100vh - 2 * var(--nav-height));
    }

    .center h1 {
        margin: 1rem;
        font-size: 4rem;
    }

    .center p {
        margin: 0.5rem;
        font-size: 1.5rem;
    }

    footer {
        display: flex;
        justify-content: center;
        align-items: center;
        color: var(--muted);
        height: var(--nav-height);
    }
</style>
